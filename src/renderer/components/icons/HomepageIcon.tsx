import icon from '@/static/icon.png'

function HomepageIcon(props: { className?: string }) {
  return <img src={icon} alt="" width={34} height={34} className={props.className} />
}

export default HomepageIcon
